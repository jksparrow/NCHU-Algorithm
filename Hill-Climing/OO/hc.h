#include <vector>
#include <ctime>
using namespace std;

class hc
{
    private:
            
    public:
            vector<int> init(int , int & );
            int transition(vector <int> &vec, int );
            int evaluation(vector <int> &vec, int );
            void determination(vector <int> &vec, int , int , int &);
};